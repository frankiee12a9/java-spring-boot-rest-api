org.springframework.dao.InvalidDataAccessApiUsageException: Could not locate named parameter [course_id], expecting one of []; nested exception is java.lang.IllegalArgumentException: Could not locate named parameter [course_id]

manager
      .createNativeQuery(format(FIND_BY_ID, "c.id = :course_id"), Tuple.class)
                    .setParameter("course_id", id);

SELECT 
       c.id,
            c.title,
            c.desc
          FROM course c
          WHERE c.course_id = c.id
